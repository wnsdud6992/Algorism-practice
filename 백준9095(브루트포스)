--1,2,3더하기--
점화식만 파악했다면 굉장히 쉬운 문제이다.
처음에 브루트포스라길래 단순히 모든 경우를 구현해야한다는 생각을 했지만 각 단계마다 관계가 있어
점화식을 세울 수 있는지 먼저 확인하는 작업이 필요하다.

#include<stdio.h>
int main()
{
	int d[12];

	int T, n;

	scanf("%d", &T);
	for (int i = 0; i < T; i++)
	{
		scanf("%d", &n);

		d[0] = 1;
		d[1] = 1;
		d[2] = 2;
		d[3] = 4;

		for (int i = 4; i <= n; i++)
		{
			d[i] = d[i - 1] + d[i - 2] + d[i - 3];

		}
		printf("%d\n", d[n]);
	}

	return 0;
}
