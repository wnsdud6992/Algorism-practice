--스타트링크--
주어진 빌딩의 높이와 현재 내층, 목표층, 한 버튼에 위로 올라갈 수 잇는 층수 내려갈 수 잇는 층수가 주어졌을 때, 최소한으로 도달하기.
사실 봤을 때 bfs로 풀 필요가 없다 생각했지만 분류가 bfs라 큐부터 만들고 시작했다. 1차원 배열의 큐로 bfs하는 거라 비교적 간단했다. 그러나
여전히 실수가 존재했다. 항상 교수님도 강조했듯이 sanity check가 매우 중요하다. 계속 간과하는 것 같다. 
내가 이미 목표지점에 도달해 있는 경우와 같이 매우 당연한 초기조건을 고려하지 않는 경우가 많은 것같은데 조심하자..


from collections import deque
q=deque()
f,s,g,u,d=map(int,input().split())
dy=[u,-d]
arr=[0 for _ in range(f+1)]
arr[g]=2
q.append((s,0))
def bfs():
    while q:
        y,t=q.popleft()
        if arr[y]==2: return t
        else: arr[y]=1
        for i in range(2):
            ny=y+dy[i]
            nt=t+1
            if ny>f or ny<=0 or arr[ny]==1: continue
            if arr[ny]==2: return nt
            if arr[ny]==0:
                arr[ny]=1
                q.append((ny,nt))
    return 'use the stairs'
total=bfs()
print(total)
