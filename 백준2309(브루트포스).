--일곱난쟁이--
9명의 입력값중에 합을 100이 되게 하는 7개를 구하는 문제이다.
9명을 모두 순회하면서 7명의 합이 100이 되는 값을 찾는 문제이다.
알고리즘 구현은 어렵지 않았으나 주의사항이 있다.
1. 여러개 입력 하나씩 받을때 lisr(map())형식으로 받으면 indexerror가 발생한다. 따라서 for문으로 append하여 받아야 한다. 
2. 이중 for문을 돌다가 안에꺼 break하고 밖도 break할 경우 flag를 이용하여 모든 for문을 멈춰줘야 한다.

import sys
arr=[]
for i in range(9):
    arr.append(int(sys.stdin.readline()))
    
a=b=0
for i in range(8):
    for j in range(i+1,9):
        if(sum(arr)-arr[i]-arr[j]==100):
            a=arr[i]
            b=arr[j]
arr.remove(a)
arr.remove(b)

arr.sort()
for i in arr:
    print(i)
