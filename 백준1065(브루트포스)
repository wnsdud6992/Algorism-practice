--한수--
한 수 내에서 등차수열인 수 찾는문제

n=int(input())
sum=0
for i in range(1,n+1):
    if i>=1 and i<=99:
        sum+=1
    elif i>=100 and i<=1000:
        arr=[]
        a=' '.join(str(i))  # 정수 i를 자리수 별로 나눠서 문자열화 시키기 위해 join하여 한칸씩 띄우고 다시 뭉쳐서 '1''0''0'과 같은 형태로 만들었다
        a=a.split()
        for j in range(len(a)):
            b=int(a[j])     #그 후 각 자리를 int형으로 바꾸고 배열에 append시켜 배열에 [1,0,0]이 들어가게 되었다.
            arr.append(b)
        if arr[0]-arr[1] ==arr[1]-arr[2]:
            sum+=1
print(sum)
