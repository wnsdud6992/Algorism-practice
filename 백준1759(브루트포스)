--암호만들기--
최소 모음1개 이상, 자음 2개이상으로 c개의 문자로 l개의 조합비밀번호 생성. 단, 오름차순.
combination및 배열과 문자열 sorting까지 파이썬이면 훨씬 수월한 문제이다.
이런문제에서 조건이 굉장히 중요. 최소모음, 자음개수를 생각해서 for문을 제한해야 한다.

from itertools import combinations
l,c= map(int, input().split())
arr=list(input().split())
mo=[]
ja=[]
for i in range(c):
    if arr[i]=='a' or arr[i]=='e' or arr[i]=='i' or arr[i]=='o' or arr[i]=='u':
        mo.append(arr[i])
    else:
        ja.append(arr[i])
final=[]
for i in range(1,len(mo)+1):    //모음이 1개일때부터 5개일때 까지 돌리는데 자음의 개수가 부족하여 모음이 1개이면 L개의 문자를 다 못채우는 경우를 찾아냄
    if l-i>len(ja): continue
    else: 
        z=i
        break
if l-len(mo)<2:        //최소 자음의 개수가 2개 이상이어야 하므로 5개 비밀번호에서 주어진 모음이 4개 이상이라면 사용 할 수 있는 모음의 개수를 1,2,3으로 한정짓는다.
    x=l-2
else: x=len(mo)

for i in range(z,x+1):
    com=list(combinations(mo,i))
    com1=list(combinations(ja,l-i))
    for a in com:
        for b in com1:
            k=list(a+b)                  //문자열은 sort불가. list안에 문자가 나뉘어진 경우 sort하고 합쳐줘서 원하는 형태를 구성!
            k.sort()
            k=''.join(k)
            final.append(k)
final.sort()
for i in final:
    print(i)

