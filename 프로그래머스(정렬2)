--H-index--
주어진 배열에서 h개 이상의 값이 h가 있고 나머지는 h이하인 값이 h-index일때 h-index의 최대값은?
처음에 문제를 받아들였을 때는 citations배열 내에 있는 값중에 하나가 h가 되는 줄 알았다. 그러나 h는 임의의 수로
citations의 요소중에 h보다 큰 값이 h개 이상 존재하도록 하는 최대값 h를 구하면 되는 것이었다.
코드의 효율성을 위해 while문으로 매번 반복을 돌기보다 한번의 반복문으로 해결하였다. 한번 돌면서 요소값중 h와 일치하면 이값을 반환하고
h로 만족하는데 그 값이 여유가 있다면 이전 요소에서 1을 뺀 값을 h로 선정하였다. 예를들어 0 1 4 5 6이 있으면 4 이상의 수는 3이므로 만족 못하지만
1이상의 수는 4개로 만족한다. 이 경우 4에서 1을 빼어 3을 바로 구할 수 있는 방법을 선택했다. 이때의 한가지 문제가 발생하였는데
5,5,5,5,5의 경우 5가 5개 이상이니 문제없지만 5,5,5의 경우 조건문에 들어맞는게 없어 0을 반환하게 된다. 이 경우는 배열의 길이를 반환하여 h를 구할 수 있었다.

def solution(citations):
    answer = 0
    citations.sort()
    for i in range(len(citations)-1,-1,-1):
        if citations[i]==len(citations)-i:
            answer=citations[i]    
            return answer      
        elif citations[i]<len(citations)-i:
            answer=len(citations)-i-1   
            return answer
    answer=len(citations)
    return answer
