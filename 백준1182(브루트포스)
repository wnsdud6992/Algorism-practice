--부분수열의 합--
n개의 수중에서 부분수열의 합이 s가 되는 부분수열의 개수를 출력
combinations를 이용하면 쉽게 k개로 이루어진 부분수열을 구할 수 있게 된다. 
sum(arr)은 arr이 list나 튜플일때 둘다 사용 가능하다.


from itertools import combinations
n,s=map(int,input().split())
num=0
arr=list(map(int,input().split()))
for i in range(1,n+1):
    com=combinations(arr,i)
    for j in com:
        if sum(j)==s:
            num+=1
print(num)
